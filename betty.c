#include <stdio.h>

int add_numbers(int a, int b)
{
	int sum a + b;
	
	return sum;
}

int main()
{
	int num1 = 10;
	int num2 = 20;
	int result;

	result = add_numbers(num1, num2);
	printf("The sm of %d and %d is %d\n", num1, num2, results);

	return 0;
}
